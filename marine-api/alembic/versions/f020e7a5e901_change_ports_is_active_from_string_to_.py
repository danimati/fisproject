"""Change ports.is_active from string to boolean

Revision ID: f020e7a5e901
Revises: 0c34a4884d1c
Create Date: 2026-03-01 17:04:29.103145

"""
from alembic import op
import sqlalchemy as sa


# revision identifiers, used by Alembic.
revision = 'f020e7a5e901'
down_revision = '0c34a4884d1c'
branch_labels = None
depends_on = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    # First, convert string values to boolean using proper PostgreSQL casting
    op.execute("UPDATE ports SET is_active = 'true' WHERE is_active = 'true'")
    op.execute("UPDATE ports SET is_active = 'false' WHERE is_active = 'false'")
    
    # Then alter the column type with explicit USING clause for PostgreSQL
    op.alter_column('ports', 'is_active',
               existing_type=sa.VARCHAR(length=10),
               type_=sa.Boolean(),
               existing_nullable=False,
               server_default=sa.text('true'),
               postgresql_using='is_active::boolean')
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    # First alter the column type back to string
    op.alter_column('ports', 'is_active',
               existing_type=sa.Boolean(),
               type_=sa.VARCHAR(length=10),
               existing_nullable=False,
               server_default=sa.text("'true'"))
    
    # Then convert boolean values back to string
    op.execute("UPDATE ports SET is_active = 'true' WHERE is_active = true")
    op.execute("UPDATE ports SET is_active = 'false' WHERE is_active = false")
    # ### end Alembic commands ###
